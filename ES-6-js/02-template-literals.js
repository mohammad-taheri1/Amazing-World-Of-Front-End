let firstName = "Mamad";
let lastName = "Taheri";

let fullName = `${firstName} ${lastName}`;

console.log(fullName);
let searchResults = 4;
let output = `${searchResults > 0 ? `${searchResults} results` :"No search result"}`;
console.log(output)